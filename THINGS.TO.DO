1. change s in AFTG/ PHG to reflect sd 
2. two formulas for PH cov and AFT cov, no first
3. dependent variable (mortdat) separately as list (DX,EX), no offset speciffied 
4. First fit baseline (DEoptim) then model, 
5. Use simple cpp code using array()
